name: typed_result
description: >-
  A lightweight, zero-dependency Result<T, E> type for Dart — explicit,
  type-safe success/failure handling without exceptions for expected errors.
version: 1.0.0
repository: https://github.com/payamomidvar/typed_result
issue_tracker: https://github.com/payamomidvar/typed_result/issues
topics:
  - error-handling
  - functional
  - result

environment:
  sdk: ^3.12.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
